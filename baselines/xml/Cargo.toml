[package]
name = "xml"
version = "0.0.0"
edition = "2024"
publish = false

[features]
static_defs = ["common/static_defs", "fandango-targets/static_defs"]

[dependencies]
fandango = { path = "../.." }
fandango-targets = { path = "../../targets", features = ["xml"] }

common = { path = "../common" }

[lints]
workspace = true