[package]
name = "common"
version = "0.0.0"
edition = "2024"
license.workspace = true
publish = false

[features]
static_defs = ["fandango-targets/static_defs"]

[dependencies]
fandango = { path = "../.." }
fandango-runtime = { path = "../../runtime" }
fandango-targets = { path = "../../targets" }

hashbrown.workspace = true
rand = { workspace = true, features = ["std_rng"] }

[lints]
workspace = true
